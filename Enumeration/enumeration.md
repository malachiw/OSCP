simple port scanner
```shell

```

another port scanner
```shell

```

using wpscan
```shell
wpscan --url http://<domain> --enumerate p --plugins-detection aggressive -o wpscan --api-token <api-token>
```
